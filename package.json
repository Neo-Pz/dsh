{
  "name": "iflow-dsh-plugin",
  "version": "0.1.1",
  "description": "iFlow A2A bridge for DeepSeek Harness (v20: P1 trust root + P2 delegation + P4 token metering).",
  "type": "module",
  "main": "lib/index.js",
  "files": [
    "lib",
    "rust",
    "cordis.patch.yml",
    "README.md"
  ],
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=esm --platform=node --target=node22 --external:@deepseek-ai/dsh-tools --external:@deepseek-ai/dsh-llm --external:@deepseek-ai/dsh-session --external:@deepseek-ai/cordis --outfile=lib/index.js",
    "prepare": "node scripts/fetch-iflow-id.mjs"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "peerDependencies": {
    "@deepseek-ai/dsh-tools": "*",
    "@deepseek-ai/dsh-llm": "*",
    "@deepseek-ai/dsh-session": "*",
    "@deepseek-ai/cordis": "*"
  },
  "devDependencies": {
    "esbuild": "^0.25.6"
  }
}
